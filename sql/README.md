# SQL implementation

Planned sequence: `00_profile/`, `01_control/`, `02_bronze/`, `03_silver/`, `04_gold/`, `05_analytics/`, with layer checks in `validation/`. Directories will be populated as gates pass. Resolve actual names in the layer below and use fully qualified references. No transformation SQL is implemented yet.
