# 06 — Analytics

One dataset per approved business question, built from Gold only. Analytics never reads Silver or Bronze.

| File | Question | Status |
|---|---|---|
| `10_activity_by_time_and_zone.sql` | Q1 — when and where activity is highest | Placeholder |
| `20_trip_behavior_by_weather.sql` | Q2 — how weather is associated with activity and behavior | Placeholder |
| `30_mobility_patterns_by_zone.sql` | Q3 — which areas show the strongest mobility patterns | Placeholder |
| `90_validate_analytics.sql` | Analytics gate: spot checks and reconciliation with Gold | Placeholder |

Table names go in `config/naming.yml` once the datasets are approved. Pickup and drop-off roles stay in separate results.
